// chrome.storage.local.set(state);

// chrome.storage.local.set({'state': false});

// chrome.storage.local.get(['state', 'text'], function(result){
//     if(result.state){
//         // console.log(result.state);
//         state = result.state;
//     }
//     if(result.text){
//         captureButton.textContent = result.text;
//     }
// })